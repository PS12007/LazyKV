# Research log

Dated, append-only notes on what was learned and what changed. Numbers are rendered from
`results/**/metrics.json`.

---

## 2026-09-15: Phase 2, blocks and GPU-only policies

### What the budget sweep says about the design

At {{ phase2.analysis.context | int }} tokens the full cache answers
{{ phase2.analysis.full_niah_accuracy | pct }} of {{ phase2.analysis.niah_prompts_per_condition }} needle prompts.
**No GPU-only eviction policy retains ≥ 99% of that at any budget below 100%.** The best,
window + sink, keeps {{ phase2.analysis.headline.window_sink.best_retention_below_full | pct0 }} of it at a
{{ phase2.analysis.headline.window_sink.best_retention_budget | pct0 }} budget, and accuracy falls with the
budget because a window keeps the last *f* of the prompt: a needle earlier than that is evicted at the
prefill→decode boundary, before any query exists. That is the mechanism Phase 3's query-aware
selection has to break, and it is why selection acts per step rather than at a boundary.

Two things are now measured rather than assumed:

- **The attention sink is not optional.** Without it, teacher-forced top-1 agreement with the full
  cache is {{ phase2.analysis.summary.window_tf_top1.min | pct }}–{{ phase2.analysis.summary.window_tf_top1.max | pct }}
  at every budget, and mean KL is {{ phase2.analysis.summary.window_tf_kl.min | .1f }}–{{ phase2.analysis.summary.window_tf_kl.max | .1f }} nats.
  With it, the same windows keep {{ phase2.analysis.summary.window_sink_tf_top1.min | pct }}–{{ phase2.analysis.summary.window_sink_tf_top1.max | pct }}
  and {{ phase2.analysis.summary.window_sink_tf_kl.min | .1e }}–{{ phase2.analysis.summary.window_sink_tf_kl.max | .1e }} nats.
- **Block-level LRU has no room to act in a GPU-only cache.** It scored identically to a no-sink
  window on all {{ phase2.analysis.lru_vs_window.prompts_compared }} prompt × budget pairs. Recency at the
  boundary is insertion order, and what it evicts there is gone before a use could be observed. The
  policy was written knowing this; the measurement is reported rather than the policy retuned.

The latency picture matches Phase 1's host-bound decode: eviction costs
{{ phase2.analysis.summary.over_full.window_sink.min | .2f }}–{{ phase2.analysis.summary.over_full.window_sink.max | .2f }}×
the full cache's per-token time, while *observing* attention for LRU costs another
{{ phase2.analysis.summary.observe_ms_per_token_lru.min | .1f }}–{{ phase2.analysis.summary.observe_ms_per_token_lru.max | .1f }} ms
per token. Any Phase 3+ policy that inspects attention pays that kind of price in kernel launches.

### Measurement incidents (kept because they would have become findings)

1. **The LRU observer was copying the cache.** Its first implementation cast every resident key to
   float32 and broadcast a matmul over the query-group axis, so its cost grew with the budget and
   would have been reported as "LRU is expensive at large budgets". The fix keeps the group axis in
   the matrix rows and upcasts only the scores. The whole sweep was rerun from one commit; the
   pre-fix results are kept in `results/phase2/*_observer_fp32/` and are used in no table.
2. **A pool that ended mid-bucket.** The bucketed cuDNN decode pads its view to the next bucket
   boundary inside existing storage. A pool sized exactly to its budget ended short of that boundary,
   so every step rebuilt a plan. Pool storage now rounds up to a bucket, and a test guards it.
3. **A chat template that stamps today's date.** It changes prompt tokens, and therefore prompt
   lengths, from day to day. It is pinned.

---

## 2026-09-14: Phase 1, trustworthy baseline

### What the baseline says about the design

The per-layer window a layer-ahead prefetch can overlap is
{{ phase1.analysis.window_summary.layer_window_min_s | ms }}–{{ phase1.analysis.window_summary.layer_window_max_s | ms }}
on the GPU timeline, roughly constant in context length, and
{{ phase1.analysis.window_summary.window_over_phase0_bound_min | .0f }}–{{ phase1.analysis.window_summary.window_over_phase0_bound_max | .0f }}×
the attention-only lower bound Phase 0 used. At 65,536 tokens, one layer's KV for
{{ phase1.analysis.window_summary.longest_ctx_fetchable_fraction | pct0 }} of the context fits
inside it, against {{ phase1.analysis.window_summary.longest_ctx_phase0_fraction | pct }} in the Phase 0
estimate. The selective-prefetch design has more room than assumed. But the window is mostly host-side
kernel launching at batch size 1, so it is a property of today's Python decode loop, not of
the GPU. The attention kernel is {{ phase1.analysis.contexts.0.attention_kernel_share_of_layer | pct0 }}
of a layer at 4,096 tokens and {{ phase1.analysis.contexts.4.attention_kernel_share_of_layer | pct0 }}
at 65,536.

### Measurement incidents (kept because they would have become findings)

1. **The reference disagreed with itself.** Teacher-forced self-comparison on the cuDNN path gave
   top-1 agreement {{ phase1.analysis.quality_first_run_summary.self_repeat_top1.min | pct }}–{{ phase1.analysis.quality_first_run_summary.self_repeat_top1.max | pct }}.
   In situ, cuDNN's single-query SDPA gave more than one bit pattern on
   {{ phase1.analysis.kernel_repeatability.by_strategy.cudnn_bucketed.not_repeatable }} of
   {{ phase1.analysis.kernel_repeatability.by_strategy.cudnn_bucketed.calls | int }} decode calls
   (`cudnn_bucketed`), and on none for the memory-efficient and math kernels. The variants
   are equally close to float64. PyTorch's determinism flags do not change it. Quality is now scored on the
   memory-efficient kernel, where the self-comparison is exact. Without this, every Phase 2
   policy would have carried a several-percent top-1 "cost" that no policy caused.
2. **Windows put the benchmark on E-cores.** Per-token latency switched between two regimes
   for seconds at a time, at every context length, with the GPU at full clock. A 2×2
   experiment separated it: OS-managed power throttling was
   {{ phase1.analysis.latency_regimes.throttling_median_ratio | .2f }}× slower at the median, and its slow
   tokens ran on P-cores {{ phase1.analysis.latency_regimes.factor_throttling.os_default.p_core_fraction_slow_tokens | pct }}
   of the time. nvidia-smi polling made no difference. Benchmarks now opt out, and the baseline was rerun.
   Without the fix, any policy that changed how long the process looked idle to Windows would have
   changed its core class too.
3. **Silent shared-memory spill, cuDNN plan rebuilds, NaN padding, host-RAM OOM kill.** Each was
   caught, fixed with a guard or test, and is described in `docs/phases/PHASE_1.md`.

### Owner decisions (resolved 2026-09-14)

- Both measurement changes accepted. License: MIT. Weights: the pinned `unsloth` mirror.
- Latency is reported as the overhead a policy adds, on the 1B model. The 3B model stays in Phase 5, pending a download go-ahead.

---

## 2026-09-14: Phase 0, feasibility

### Which design the measurements support

Measured on this laptop, the VRAM-to-PCIe bandwidth ratio is
**{{ phase0.analysis.vram.ratio_d2d_over_pinned_h2d.median | x }}**, not the 20–40× the brief
assumed. Pinned H2D reaches {{ phase0.analysis.pcie.pinned_h2d.asymptote_bps.median | gbps }}
over a Gen5 x8 link. The brief's claim that fetching offloaded KV is an "arithmetic
impossibility" is therefore wrong as stated. Its conclusion survives on a different
comparison. Fetching a layer's non-resident KV costs
{{ phase0.analysis.design.fetch_over_attention_long_ctx.min | x }}–{{ phase0.analysis.design.fetch_over_attention_long_ctx.max | x }}
the time of the best GPU attention kernel over the same tokens
({{ phase0.analysis.design.fetch_over_attention_long_ctx.min_tokens_per_layer | int }}–{{ phase0.analysis.design.fetch_over_attention_long_ctx.max_tokens_per_layer | int }}
tokens per layer). Compute and H2D overlap almost fully, but only
{{ phase0.analysis.design.fetch_over_attention_long_ctx.fetchable_fraction_min | pct }}–{{ phase0.analysis.design.fetch_over_attention_long_ctx.fetchable_fraction_max | pct }}
of a layer's tokens can be fetched within that window. That figure is a lower bound, since MLP time was
not measured. **Design (b) plus query-aware block selection with selective prefetch is
supported. Dense fetch (a) is not.** Exact CPU partial attention (c) costs
{{ phase0.analysis.design.rows.4.cpu_partial_attn_all_layers_s | ms }} per token at 65,536
non-resident tokens, and it beats fetching only when the CPU tier's layout forces a host
gather or per-block transfers. It stays as a small-set exactness experiment, not a main
path. Arithmetic: `docs/IMPLEMENTATION_PLAN.md` §3.

### Measurement incidents (kept because they would have become findings)

1. **Kernel-selection trap.** This Windows torch build has no FlashAttention. Default SDPA
   dispatch with GQA silently uses the math kernel, which is
   {{ phase0.analysis.vram.decode_attention.2.default_over_best | x }} slower than cuDNN
   attention at 65,536 tokens. The math kernel scans KV more slowly than the PCIe link
   transfers it (`SYSTEM_INFO.md` §2 and §3). Timed that way, GPU attention would have looked
   slower than fetching, and design (a) would have looked viable. Fix: every backend is timed and checked against math.
2. **DVFS artifact.** A GPU timing that followed the CPU section in the first smoke test ran
   several times slower than the same operation on a warm GPU (smoke-test output not
   retained, so no number is quoted), because the laptop GPU had
   dropped to its minimum clock. Fix: spin up before every GPU section, and log clock and
   link generation throughout.
3. **Unreproduced low PCIe result.** The very first smoke test reported a pinned H2D figure
   far below every later run (2 quick and 3 full). Its cause is unknown. The output was not
   retained, so no number is quoted. Response: independent process-level runs with
   between-run ranges reported.
4. **Contaminated run discarded.** The first full run overlapped web requests made for the
   related-work survey during its overlap and CPU sections. It was deleted before analysis,
   and all three reported runs ran on an idle machine from a clean tree.

### Other things learned

- `asyncEngineCount` = {{ phase0.analysis.overlap.async_engine_count.0 }}, confirmed by timing: H2D and D2H serialize completely. CPU copies of KV must be written once, never written back.
- Host RAM is a single DIMM (single-channel). Single-thread memcpy runs at {{ phase0.analysis.cpu.host_memcpy_bps_1thread.median | gbps }}, slower than the PCIe link, so host-side gathers are a real cost.
- A 64-token per-layer block ({{ phase0.analysis.design.block_64tok_bytes_per_layer | iec }}) reaches only {{ phase0.analysis.design.block_64tok_fraction_of_asymptote | pct0 }} of PCIe bandwidth as one transfer.
- `expandable_segments` is not supported on this platform.
- Llama-3.2-1B's full 64K bf16 KV fits in free VRAM, so the study's GPU budget is imposed, not forced. The 3B model does not fit in bf16 even at modest context.

### Decisions needed from the project owner

- Accept the corrected justification for §B1 (rule 8). The direction is unchanged.
- Llama 3.2 weight access: provide an HF token, or approve the ungated mirror.
- Pick a license for the public repository.
